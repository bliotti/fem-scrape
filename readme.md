## run the extension on any page with `a` tag linking to a video

## wait for results in popup

## copy results and paste them into test.txt

### only run once in terminal `chmod +x download.sh`

## then run `./download.sh`
